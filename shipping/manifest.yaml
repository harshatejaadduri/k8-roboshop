apiVersion: v1 
kind: configMap
metadata:
 name: shipping
 namespace: roboshop
 labels:
  project: roboshop
  environment: dev
  tier: backend
type: Opaque
data:
 CART_ENDPOINT: "cart:8080" 
 DB_HOST: "mysql"

---
apiVersion: apps/v1
kind: Development
metadata:
 name: shipping
 namespace: roboshop
 labels:
  project: roboshop
  environment: dev
  tier: backend
spec:
 replicas: 1
 selectors:
  matchLabels:
    project: roboshop
    environment: dev
    tier: backend
 template:
  metadata:
   name: shipping
  spec:
   containers:
    - name: shipping
      image: harshatejaadduri/shipping:v2
      envFrom:
       - configMapRef:
         name: shipping

---
apiVersion: v1 
kind: Service
metadata: 
 name: shipping
 namespace: roboshop
spec:
 selector:
  project: roboshop
  environment: dev
  tier: backend
 ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
          